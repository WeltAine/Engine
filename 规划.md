# System、Pipeline、Schedule 与序列化重构规划

## 1. 文档目的

本文记录从提交 `e38c60ab4866913b5d4a999a882ba3fde0fd99c4` 到当前重构起点 `810fa84f2b91c400b48933eef9460aa2836a2948` 所建立的 System 系统、Pipeline、Schedule、World 生命周期和序列化机制，并确定下一阶段重构的设计约束与实施顺序。

本轮重构的主要目标：

- 明确 Builder、Pipeline、Schedule、World、Registry 和 Serialization 的职责；
- 保证 System 生命周期完整，避免重复或遗漏 `OnBegin`、`OnEnd`、`OnAttach`、`OnDetach`；
- 让实时 Schedule 可以被 Editor 观察和交互，但不能被外部结构性破坏；
- 让 Builder 成为唯一的管线结构编辑模型；
- 整理 JSON DTO、Glaze metadata 和序列化映射，减少头文件耦合；
- 为未来的阶段独立 Order、System 依赖图、并行调度和 Generation Handle 保留扩展空间；
- 优先保证设计简洁、易读、可测试，不为尚未实现的功能建立过度抽象。

---

## 2. 核心术语与职责

### 2.1 SystemRegistry

负责：

- 维护稳定的 System TypeKey；
- 维护进程内 RuntimeId；
- 提供 Editor 使用的 DisplayName；
- 创建 System 实例；
- 提供默认 `SystemSpecification`；
- 序列化和反序列化 System 的可持久化配置。

不负责：

- 构建 Pipeline；
- 修改 Builder；
- 驱动 Schedule；
- 直接认识 JSON DTO；
- 管理 World 生命周期。

### 2.2 SystemPipeline::Builder

唯一允许结构编辑的模型，负责：

- 添加和删除 System；
- 修改 PhaseMask、ModeMask 和 Order；
- 修改 System 的持久化配置；
- 为 Editor 提供线性列表；
- 保存尚未解析的 System 定义；
- 在 Build 时执行完整验证。

### 2.3 SystemPipeline

Builder Build 后得到的不可变定义，负责：

- 保存规范化后的 SystemDefinition 列表；
- 保持宏观 Pipeline 顺序；
- 作为构建运行时 Schedule 的输入；
- 可重复构建新的 Schedule。

Pipeline 不拥有 System 运行实例，也不管理生命周期。

### 2.4 SystemSchedule

运行时执行对象，负责：

- 拥有 System 实例；
- 管理 System Attach、Begin、Update、End、Detach；
- 保存 Begin 和各更新阶段的编译执行计划；
- 向 Editor 提供受控的实时 System 访问；
- 在构建完成后保持拓扑不可变。

Schedule 不负责：

- 添加、删除或重排 System；
- 修改 PhaseMask、ModeMask 或 Order；
- 保存 JSON DTO；
- 提供文件 I/O。

### 2.5 World

System 生命周期的外部入口，负责：

- Begin、Update、End 和模式切换；
- 拥有当前 Scene 和 Schedule；
- 事务化应用新的 Pipeline；
- 在 Apply 后恢复原来的运行模式；
- 防止外部绕过生命周期直接替换 Schedule。

### 2.6 Serialization

负责领域对象和 JSON 文档之间的映射：

```text
SystemSchedule -> SystemPipelineDocument
SystemPipelineDocument -> SystemPipeline::Builder
World -> WorldDocument -> JSON/String/File
```

JSON DTO 只保存数据，不主动查询 Registry，不包含到领域对象的转换运算符。

---

## 3. 总体关系

```text
                         +--------------------+
                         | SystemRegistry     |
                         | 类型/工厂/配置 Codec |
                         +----------+---------+
                                    |
                                    v
+---------------------+   Build   +---------------------+
| Pipeline::Builder   | --------> | SystemPipeline      |
| 可编辑结构           |           | 不可变定义           |
+---------------------+           +----------+----------+
                                             | Compile
                                             v
                                  +---------------------+
                                  | SystemSchedule      |
                                  | 实例和执行计划        |
                                  +----------+----------+
                                             | owned/driven
                                             v
                                  +---------------------+
                                  | World               |
                                  +---------------------+

Editor:
    观察 World 中的实时 Schedule
    通过 View<ISystem> 与实时 System 交互
    明确进入结构编辑时创建独立 Builder
    Apply 时通过 World 事务化重建 Schedule
```

---

## 4. 已冻结的设计决策

| 问题 | 决定 |
|---|---|
| Builder 容器 | `std::vector<SystemDefinition>` |
| Builder 顺序 | 按 `SystemSpecification::Order` 稳定排序 |
| 相同 Order | 保留 Builder 中的相对顺序 |
| 唯一性 | Builder 显式检查，Build 时再次验证 |
| Pipeline 容器 | 规范化后的不可变 vector |
| 持久化身份 | 稳定 TypeKey |
| 运行时身份 | RuntimeId，仅限进程内使用 |
| Editor 名称 | DisplayName，与 TypeKey 分离 |
| Attach 顺序 | Pipeline 宏观顺序 |
| Detach 顺序 | 实际成功 Attach 顺序的逆序 |
| Begin 顺序 | 当前使用 Order，但保存为独立执行计划 |
| End 顺序 | 本次实际成功 Begin 顺序的逆序 |
| Phase 执行 | 每个 Phase 使用独立的 `vector<SystemIndex>` |
| Editor 实时交互 | `View<ISystem>` 或受控 RuntimeView |
| 结构编辑 | 只能通过 Builder |
| mask JSON | 具体枚举字符串数组 |
| `None` | 空数组 |
| `AllSceneMode` | 展开为具体原子模式 |
| Glaze metadata | 保留，但移至 JSON Adapter 头文件 |
| 预期错误 | Result/领域结果对象 |
| 意外异常 | 在模块接缝处捕获并转换为错误 |
| World Apply | 候选对象完整构建后事务提交 |
| Scene 重构 | 本轮只做事务加载所需的最小修改 |

---

## 5. Builder 和 Pipeline 数据模型

### 5.1 SystemDefinition

建议领域结构：

```cpp
struct SystemDefinition
{
    SystemTypeKey Type;
    SystemSpecification Specification;
    SystemConfiguration Configuration;
};
```

其中：

- `Type` 是持久化使用的稳定 TypeKey；
- `Specification` 保存 PhaseMask、ModeMask 和宏观 Order；
- `Configuration` 保存 System 的可持久化配置；
- 核心领域结构不直接持有 `glz::raw_json`。

第一阶段可以让 `SystemConfiguration` 内部保存 JSON 字符串：

```cpp
struct SystemConfiguration
{
    std::string Json = "{}";
};
```

这样 Pipeline 不直接依赖 Glaze，同时仍可保存从 JSON 加载的配置。

### 5.2 Builder 容器

```cpp
class SystemPipeline::Builder
{
public:
    using DefinitionList = std::vector<SystemDefinition>;

private:
    DefinitionList m_Definitions;
    int m_NextOrder = 0;
};
```

不长期维护额外的 map 索引。System 数量预计较少，使用线性查找保持实现简单。

### 5.3 排序规则

```cpp
std::stable_sort(
    m_Definitions.begin(),
    m_Definitions.end(),
    [](const SystemDefinition& left,
       const SystemDefinition& right)
    {
        return left.Specification.Order <
               right.Specification.Order;
    });
```

规则：

- Order 不同：按 Order 排序；
- Order 相同：保留原相对顺序；
- 不使用 RuntimeId 作为相同 Order 的持久化 tie-break；
- JSON 数组顺序可以保留相同 Order System 的相对顺序。

### 5.4 唯一性

容器不负责表达唯一性。Builder 显式保证：

```text
一个 Pipeline 中同一个 TypeKey 只能出现一次。
```

Add 时检查一次，Build 时使用临时 `unordered_set<SystemTypeKey>` 再次验证。

### 5.5 Order 的当前含义

当前 `SystemSpecification::Order` 定义为：

> System 在 Pipeline 中的宏观顺序，同时作为当前版本 Begin 和所有更新阶段的默认顺序。

本轮不实现阶段独立 Order，但 Schedule 内部不会假设不同执行计划必须永远同序。

---

## 6. Schedule 结构与生命周期

### 6.1 内部存储

建议结构：

```cpp
class SystemSchedule
{
private:
    std::vector<SystemEntry> m_Systems;

    std::vector<SystemIndex> m_AttachSequence;
    std::vector<SystemIndex> m_BeginPlan;
    std::vector<SystemIndex> m_BegunSystems;

    std::array<
        std::vector<SystemIndex>,
        SystemPhaseCount
    > m_PhasePlans;

    ScheduleState m_State = ScheduleState::Ready;
    Scene* m_SessionScene = nullptr;
    SceneMode m_SessionMode = SceneMode::None;
};
```

### 6.2 移除 PhaseSystemEntry 继承

移除：

```cpp
struct PhaseSystemEntry : ISystem;
```

Phase 计划只保存 `SystemIndex`：

```cpp
for (SystemIndex index : m_PhasePlans[phase])
{
    m_Systems[index].Instance->OnUpdate(context);
}
```

收益：

- 不需要阶段裸指针；
- 不需要复制 RuntimeId、Order 和 ModeMask；
- 不需要 `const_cast`；
- Schedule move 后索引仍然有效；
- 未来每阶段独立 Order 只需重新编译对应计划。

### 6.3 生命周期契约

```text
OnAttach：只有成功返回后才记录为 Attached
OnBegin：只有成功返回后才记录为 Begun
OnEnd：按本次成功 Begin 序列逆序
OnDetach：按成功 Attach 序列逆序
```

Schedule 状态至少包括：

```cpp
enum class ScheduleState
{
    Ready,
    Running
};
```

规则：

- Ready 才允许 Begin；
- Running 才允许 Run；
- Running 才执行正常 End；
- 重复 Begin 被拒绝；
- 重复 End 不重复调用回调；
- 析构发现仍在 Running 时先兜底 End，再 Detach；
- 析构和清理阶段不允许异常向外逃逸。

### 6.4 外部接口约束

删除或私有化：

```cpp
AddSystem
RemoveSystem
Clear
std::vector<SystemEntry>& GetSystems()
SystemSchedule& World::GetSystemSchedule()
```

World 对外只返回结构只读的 Schedule 或 RuntimeView。

---

## 7. Editor 实时交互

Schedule 拓扑只读，但其中的 System 实例可以实时交互。

建议视图：

```cpp
struct SystemRuntimeView
{
    const SystemInformation& Information;
    const SystemSpecification& Specification;
    View<ISystem> Instance;
};
```

允许：

- 调用 `Instance->OnGui()`；
- 修改 System 自己公开的实时配置；
- 查看 System 调试数据；
- 查看生命周期和运行状态。

不允许：

- 替换 `Instance`；
- 添加或删除 System；
- 修改 PhaseMask、ModeMask 或 Order；
- 直接重排 Schedule；
- 获取 `Scope<ISystem>` 或 Schedule 内部容器。

结构编辑流程：

```text
点击 Edit
    -> 从实时 Schedule 捕获配置并创建 Builder
    -> Builder 独立编辑
    -> Cancel：丢弃 Builder
    -> Apply：Builder Build Pipeline，World 事务化替换 Schedule
```

---

## 8. Registry 与稳定类型标识

### 8.1 身份拆分

```text
TypeKey：稳定字符串，用于磁盘、项目和 Editor
RuntimeId：进程内快速比较
DisplayName：Editor 显示，可调整或本地化
```

禁止继续将 `typeid(System).name()` 作为持久化名称。

示例：

```text
TypeKey     = "Ayin.RenderSystem"
DisplayName = "Render System"
RuntimeId   = entt::type_hash<RenderSystem>::value()
```

### 8.2 SystemDescriptor

建议：

```cpp
struct SystemDescriptor
{
    SystemTypeId RuntimeId;
    std::string TypeKey;
    std::string DisplayName;
    SystemSpecification DefaultSpecification;

    std::function<Scope<ISystem>()> Create;

    std::function<SerializeConfigurationResult(const ISystem&)>
        SerializeConfiguration;

    std::function<DeserializeConfigurationResult(
        ISystem&,
        std::string_view)>
        DeserializeConfiguration;
};
```

回调接收 System 引用，不接收 `Scope<ISystem>&`，避免 Registry 依赖调用者的所有权实现。

Registry 不再包含到 Pipeline 定义的转换运算符。

---

## 9. JSON DTO 与枚举 mask

### 9.1 mask 表达

Pipeline JSON 中不保存整数 mask：

```json
{
  "Phases": ["Update", "Presentation"],
  "Modes": ["Editor", "Simulation", "Runtime"]
}
```

不保存：

```json
{
  "PhaseMask": 10,
  "ModeMask": 7
}
```

### 9.2 Glaze metadata

保留：

```cpp
template<>
struct glz::meta<Ayin::SceneMode>;

template<>
struct glz::meta<Ayin::SystemPhase>;
```

Glaze metadata 负责：

```text
单个枚举值 <-> JSON 字符串
```

mask 映射负责：

```text
枚举 mask <-> vector<枚举值>
```

二者职责不同，缺一不可。

### 9.3 规范写入规则

```text
SceneMode::None        -> []
SystemPhase::None      -> []
AllSceneMode           -> ["Editor", "Simulation", "Runtime"]
```

`AllSceneMode` 只作为 C++ 便利常量，规范写入器不输出 `"AllSceneMode"`，避免未来新增模式时产生语义歧义。

### 9.4 合法原子枚举列表

持久化映射使用明确的合法值列表：

```cpp
constexpr std::array SceneModeValues{
    SceneMode::Editor,
    SceneMode::Simulation,
    SceneMode::Runtime
};

constexpr std::array SystemPhaseValues{
    SystemPhase::PreUpdate,
    SystemPhase::Update,
    SystemPhase::PostUpdate,
    SystemPhase::Presentation
};
```

序列化前验证未知 bit；反序列化时验证：

- 未知枚举值；
- 重复枚举值；
- `None` 与其他值混合；
- 不支持的组合值。

### 9.5 metadata 文件位置

将运行时枚举的 Glaze metadata 移至序列化 Adapter，例如：

```text
Ayin/src/Ayin/Serialization/Json/EngineEnumJson.h
```

运行时核心头文件不再直接承担 JSON 格式知识。

---

## 10. JSON 文档结构

建议：

```cpp
namespace Ayin::Serialization::Json
{
    struct SystemDocument
    {
        std::string Type;
        std::vector<SystemPhase> Phases;
        std::vector<SceneMode> Modes;
        int Order = -1;
        glz::raw_json Configuration = "{}";
    };

    struct SystemPipelineDocument
    {
        uint32_t FormatVersion = 1;
        std::vector<SystemDocument> Systems;
    };
}
```

JSON DTO：

- 只包含字段和 Glaze metadata；
- 不包含 `operator SystemDefinition()`；
- 不包含 `operator SystemJson()`；
- 不主动访问 Registry；
- 不直接修改 Builder、Pipeline 或 Schedule。

映射由独立 Serialization 模块负责。

---

## 11. System 配置与运行状态

System 序列化只保存可编辑配置，不保存任意运行时状态。

例如：

```cpp
class RenderSystem : public ISystem
{
private:
    float m_Exposure;               // 可持久化
    bool m_EnableDebugOverlay;      // 可持久化

    Framebuffer* m_CurrentTarget;   // 不持久化
    uint64_t m_FrameCounter;        // 不持久化
    Entity m_CurrentCamera;         // 不持久化
};
```

System 自己的 Glaze metadata 或配置 Codec 只包含应持久化字段。

---

## 12. 错误处理与异常策略

### 12.1 预期错误

以下情况使用 Result 或领域结果对象，不使用异常作为正常控制流：

- 文件不存在；
- JSON 语法错误；
- 未知 System；
- 重复 TypeKey；
- 配置字段错误；
- mask 非法；
- 格式版本不支持；
- Pipeline Build 失败；
- World Apply 失败。

第一阶段优先使用易读的领域结果对象，例如：

```cpp
struct PipelineBuildResult
{
    std::optional<SystemPipeline> Pipeline;
    std::vector<PipelineBuildError> Errors;
};
```

暂不强制引入全工程通用 `Result<T, E>`。

### 12.2 意外异常

在模块接缝处捕获第三方或用户代码抛出的异常：

- System 配置 Codec；
- System 生命周期回调；
- Schedule 构建；
- World Apply；
- World Deserialize。

### 12.3 生命周期异常清理

```text
OnAttach 失败：
    Detach 之前成功 Attach 的 System

OnBegin 失败：
    End 之前成功 Begin 的 System

OnUpdate 失败：
    返回包含 TypeKey 和 Phase 的执行错误
    结束当前运行会话

OnEnd 失败：
    记录错误，继续 End 剩余 System

OnDetach 失败：
    记录错误，继续 Detach 剩余 System

析构：
    catch 所有异常，禁止异常逃逸
```

---

## 13. World 事务化 Apply

目标：Apply 失败时旧 World 完全不变。

流程：

```text
Builder.Build()
    -> 得到候选 Pipeline
    -> 验证所有 TypeKey
    -> 创建所有候选 System 实例
    -> 反序列化所有 System 配置
    -> 构建候选 Schedule 执行计划
    -> 候选对象全部成功
    -> 记录当前 SceneMode
    -> End 旧 Schedule
    -> Detach 旧 Schedule
    -> 提交候选 Schedule
    -> 如果之前正在运行，按原 SceneMode Begin 新 Schedule
```

所有可能失败的验证和配置反序列化尽量在旧 Schedule 停止前完成。

候选对象使用 RAII 管理，失败时自动销毁，不需要手写 rollback。

`World::ResetSchedule()` 最终替换为有结果返回的 Apply 接口。

---

## 14. World 事务化反序列化

当前 World 反序列化存在提前修改 Scene 和部分提交风险。

目标流程：

```text
JSON String/File
    -> WorldDocument
    -> 验证格式版本
    -> 构建候选 Scene
    -> SystemPipelineDocument -> Builder
    -> Builder -> Pipeline
    -> Pipeline -> 候选 Schedule
    -> 全部成功后一次性提交 World
```

失败时：

- 旧 Scene 不变；
- 旧 Schedule 不变；
- 当前 Mode 不变；
- 不留下半加载 Entity 或 System。

本轮只对 SceneSerializer 做支持候选 Scene 和错误传播所需的最小修改，不全面重写 Component/Scene 序列化。

---

## 15. Serializer 中的 View

同步 Serializer 不应无条件延长 World 或 Scene 的生命周期。

优先级：

```text
单次同步操作：函数参数使用引用
跨多个同步成员函数暂存目标：使用 View<T>
异步操作：使用 Ref<const T> 或不可变 Snapshot
```

例如：

```cpp
SerializeResult Serialize(const World& world);
DeserializeResult Deserialize(World& world, std::string_view json);
```

如果保留状态化 Serializer：

```cpp
View<World> m_World;
View<Scene> m_Scene;
```

`View<T>` 是非拥有指针包装，不提供悬空检测。调用方必须保证目标生命周期覆盖整个同步操作。

---

## 16. 建议文件结构

```text
Ayin/src/Ayin/System/
    SystemTypes.h
    SystemContext.h
    ISystem.h

    Systems.h
    Systems.cpp

    SystemRegistry.h
    SystemRegistry.cpp

    SystemPipeline.h
    SystemPipeline.cpp

    SystemSchedule.h
    SystemSchedule.cpp

    World.h
    World.cpp

Ayin/src/Ayin/Serialization/Json/
    EngineEnumJson.h
    SystemPipelineJson.h
    WorldJson.h

Ayin/src/Ayin/Serialization/
    SystemPipelineSerialization.h
    SystemPipelineSerialization.cpp

    WorldSerializer.h
    WorldSerializer.cpp
```

SceneSerializer 暂时保留在 Scene 目录，后续单独整理。

---

## 17. 实施阶段与验收标准

### 阶段 0：建立测试基线

工作：

- 运行现有 System 测试；
- 增加重复 Begin、重复 End、Run before Begin 测试；
- 增加运行中 Reset 测试；
- 增加 Attach/Detach 和 Begin/End 精确次数测试；
- 增加 mask JSON 测试；
- 增加序列化 round-trip 测试骨架。

验收：

- 当前行为有明确记录；
- 后续行为变化由测试和设计共同说明。

### 阶段 1：拆分基础头文件

工作：

- `SystemTypes.h`；
- `SystemContext.h`；
- `ISystem.h`；
- `SceneMode.h`；
- `Systems.h` 只保存内置 System；
- 清理不必要 include。

验收：

- 每个公开头文件可独立 include 并通过 MSVC `/Zs`；
- System 运行时核心类型不依赖 Glaze；
- System 子目录没有直接 include cycle。

### 阶段 2：重构 Registry

工作：

- 稳定 TypeKey；
- DisplayName；
- RuntimeId；
- 工厂回调；
- 配置编解码回调；
- 重复注册验证；
- 错误和异常转换；
- Registry 与 Pipeline 解耦。

验收：

- Registry 不认识 SystemDefinition 和 JSON DTO；
- 未知 TypeKey 返回错误，不发生空指针解引用；
- Debug/Release 使用相同持久化 TypeKey。

### 阶段 3：重构 Builder/Pipeline

工作：

- 引入 SystemDefinition；
- Builder 容器改为 vector；
- 显式唯一性；
- stable sort；
- Build 完整验证；
- Pipeline 不可变；
- 移除核心模型中的 `glz::raw_json`。

验收：

- Builder 可以直接支持 Editor 线性显示；
- 相同 Order 保持相对顺序；
- Build 不会静默忽略错误；
- Pipeline 构建后不能结构修改。

### 阶段 4：重构 Schedule

工作：

- 移除 PhaseSystemEntry 继承；
- Phase 计划改为 SystemIndex；
- 增加 AttachSequence、BeginPlan、PhasePlans；
- 增加 Ready/Running 状态；
- 删除公开 Add/Remove/Clear；
- 删除可修改容器访问；
- 完善部分失败和异常清理。

验收：

- 不重复 OnBegin、OnEnd、OnDetach；
- 析构中的异常不逃逸；
- 运行中替换会先 End 再 Detach；
- 外部无法结构性破坏 Schedule。

### 阶段 5：实时访问接口

工作：

- SystemRuntimeView；
- 使用现有 `View<ISystem>`；
- Editor 可调用 System `OnGui()`；
- Specification 和 Information 对 Editor 只读。

验收：

- Editor 能与实时 System 交互；
- Editor 不能替换或删除 Schedule 中的 System。

### 阶段 6：System JSON 与 mask 映射

工作：

- EngineEnumJson；
- SystemPipelineDocument；
- 格式版本；
- mask 原子数组映射；
- Schedule -> Document；
- Document -> Builder；
- 旧字段迁移策略。

验收：

- mask 写为可读字符串数组；
- `None` 写为空数组；
- `AllSceneMode` 展开；
- 未知 bit、重复值和非法组合返回错误；
- Schedule -> Document -> Builder -> Pipeline -> Schedule round-trip 正确。

### 阶段 7：World Apply

工作：

- 候选 Schedule；
- ApplyResult；
- 旧 Schedule 生命周期结束；
- 新 Schedule 模式恢复；
- Apply 强异常保证。

验收：

- Apply 失败时旧 World 不变；
- Apply 成功时生命周期顺序正确；
- 活动模式正确恢复。

### 阶段 8：World Deserialize

工作：

- 候选 Scene；
- 候选 Pipeline/Schedule；
- 完整错误传播；
- 全部成功后提交；
- 消除空 Scene 解引用路径。

验收：

- 任一环节失败都不会留下部分加载状态。

### 阶段 9：文档、清理与 Editor 接缝

工作：

- 更新根 README；
- 删除旧兼容接口；
- 更新 `Ayin.h`；
- 更新 vcxproj 和 filters；
- 更新测试文档；
- 记录长期路线图。

验收：

- 新接口成为唯一推荐路径；
- 旧的结构性 Schedule 修改接口不存在；
- 全部测试和 Debug 构建通过。

---

## 18. 测试计划

### 生命周期

- Attach 每个 System 一次；
- Detach 每个 System 一次；
- Begin 重复调用被拒绝；
- Run before Begin 被拒绝；
- End 重复调用不重复回调；
- World 析构时兜底 End 和 Detach；
- Attach/Begin 部分失败时正确逆序清理；
- Update 异常返回 System 和 Phase 信息。

### Builder/Pipeline

- 重复 TypeKey；
- 未知 System；
- 自动 Order；
- 显式 Order；
- 相同 Order 稳定顺序；
- SetOrder 后重新排序；
- Builder Build 后仍可继续编辑；
- Pipeline 不可修改。

### Registry

- 稳定 TypeKey；
- 重复注册；
- RuntimeId 冲突；
- 工厂失败；
- 配置编解码；
- 配置回调抛异常。

### 序列化

- 空 Pipeline；
- 单 System；
- 多 System；
- Phase/Mode 组合；
- 嵌套 Configuration JSON；
- 未知 System 保留或错误策略；
- 格式版本；
- mask 非法 bit；
- 重复枚举值；
- round-trip。

### World

- Apply 成功；
- Apply 失败回滚；
- 活动模式恢复；
- Scene 成功但 Pipeline 失败时不提交；
- Pipeline 成功但 Scene 失败时不提交；
- 完整 World save/load round-trip。

### 头文件

为所有公开 System 和 Serialization 头文件建立独立 include 编译测试，使用 MSVC `/Zs` 检查自包含性。

---

## 19. 当前明确不做

本轮不实现：

- System 多线程调度；
- System before/after 依赖；
- System 拓扑排序；
- 每个 Phase 独立 Order；
- Begin 独立 Order；
- 全工程通用反射框架；
- 全面重写 Scene/Component 序列化；
- generation handle 资源系统；
- 为消除所有裸指针而建立复杂 Handle；
- 异步 World 序列化。

这些内容不得在本轮实现中顺手扩张范围。

---

## 20. 长期路线图

### 20.1 独立执行顺序

- BeginOrder；
- PreUpdateOrder；
- UpdateOrder；
- PostUpdateOrder；
- PresentationOrder；
- 未指定时回退 PipelineOrder。

### 20.2 System 依赖与并行调度

- before/after 约束；
- 依赖图验证；
- 拓扑排序；
- 无依赖 System 并行运行；
- 调度可视化和诊断。

### 20.3 Scene/Component 序列化

- 稳定 Component TypeKey；
- DisplayName 与持久化身份分离；
- 显式 SceneSerializationContext；
- 去除全局静态序列化上下文；
- Component 错误传播；
- Script 配置与运行实例分离；
- 逐步移出 Components.h 中的 UI 和序列化实现。

### 20.4 Generation Handle 与资源池

目标模型：

```text
Handle -> Resolve -> View<T>
```

Handle 保存：

```cpp
struct Handle
{
    uint32_t Index;
    uint32_t Generation;
};
```

适用对象：

- Texture；
- Shader；
- Material；
- Mesh；
- Framebuffer；
- 异步加载资源；
- 渲染线程资源；
- Editor 跨帧选择对象。

职责分工：

```text
SystemIndex：Schedule 内部短生命周期索引
View<T>：已确认存活对象的临时非拥有访问
Generation Handle：可跨帧保存并检测失效的长期身份
```

---

## 21. 建议提交拆分

1. 测试基线和行为契约；
2. 基础类型与头文件拆分；
3. Registry 与稳定 TypeKey；
4. Builder/Pipeline 数据模型；
5. Schedule 执行计划与生命周期；
6. RuntimeView 和 Editor 实时访问；
7. System JSON、枚举 mask 与 round-trip；
8. World 事务 Apply；
9. World 事务 Deserialize；
10. README、项目文件和旧接口清理。

每个提交都应保持可编译，并运行对应阶段已有测试。不要将全部重构压入一个巨大提交。

---

## 22. 最终原则

1. World 是生命周期的外部接口；
2. Schedule 拓扑不可变，但实时 System 可以交互；
3. Builder 是唯一的结构编辑模型；
4. Pipeline 是不可变定义；
5. Attach/Detach 和 Begin/End 分别按实际成功序列正逆执行；
6. Begin 和不同 Update Phase 不要求同序；
7. 当前 Order 是宏观 PipelineOrder，未来可扩展独立阶段 Order；
8. TypeKey 用于持久化，RuntimeId 只用于运行时；
9. mask 序列化为具体枚举字符串数组；
10. JSON DTO 只保存数据；
11. 预期错误使用 Result，异常只作模块接缝兜底；
12. 候选对象完整构建后才提交到 World；
13. 优先简单、明确、可测试，不建立尚无真实需求的抽象。
