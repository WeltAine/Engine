# System、Pipeline、Schedule 与编辑会话实施规划

## 1. 文档状态

本文档以当前分支的真实代码为准，记录 System 核心、编辑会话和序列化机制目前已经完成的内容、尚未完成的问题，以及后续按阶段推进的计划。

旧阶段的详细讨论和提交过程保留在 Git 历史中，不再继续堆叠到本文档。当前分支的临时实施细节记录在 `System接口收敛实施记录.md` 中，该文件不提交。

当前基线：

```text
分支：codex/system-interface-convergence
基线：ad1a6cf94dd65a6eccaf76c778a2855d53793e44
当前实现：4104d61
```

当前实现由以下提交组成：

```text
cfaf8c1 - 重构: 统一 System Schedule 创建语义
04560b4 - 重构: 收敛 System Editor 交互接口
4104d61 - 验收: 完善 System Editor 与多 World Apply
```

本文档的目标不是描述所有未来可能的 System 功能，而是保持以下内容始终清晰：

- 当前代码实际上提供了什么；
- 当前接口之间的职责边界是什么；
- 哪些问题已经解决，哪些问题仍然存在；
- 下一阶段应该以什么顺序、用什么验收标准推进。

---

## 2. 当前架构事实

当前 System 运行与编辑相关的主要关系如下：

```text
SystemRegistry
    -> SystemPipeline::Builder
    -> SystemPipeline
    -> SystemSchedule
    -> World
    -> EditorSession
```

其中，`Ayin-Editor` 位于运行时模块之上，负责具体的交互流程，而不是由 `World` 或 `SystemSchedule` 代替它决定编辑行为。

### 2.1 SystemRegistry

`SystemRegistry` 负责：

- 保存 System 的稳定 `TypeKey`；
- 保存进程内使用的 `RuntimeId`；
- 注册 System 工厂；
- 创建 System 实例；
- 保存 System 描述和默认规格；
- 调用对应 System 的配置序列化和反序列化 Codec。

`SystemRegistry` 不负责：

- 编辑 Builder；
- 创建或驱动 World；
- 决定 System 的执行顺序；
- 保存 Pipeline JSON DTO。

### 2.2 SystemPipeline::Builder

`Builder` 是唯一的结构编辑模型，负责保存可编辑的 System 定义：

- 添加和删除 System；
- 修改 PhaseMask、ModeMask 和 Order；
- 修改 System 的持久化配置；
- 向 Editor 提供稳定的线性定义列表；
- 对重复 System、未知 TypeKey 和非法定义进行检查；
- 构建不可变的 `SystemPipeline`。

Builder 中保存的是：

```text
SystemTypeKey
SystemSpecification
SystemConfiguration
```

不保存运行时 System 实例，也不直接控制生命周期。

Builder 当前会在结构改变后保持定义列表按 Order 排序。这个顺序同时服务于 Editor 显示和 Pipeline 的宏观顺序；各 Update 阶段在 Schedule 构建时生成自己的执行计划。

### 2.3 SystemPipeline

`SystemPipeline` 是 Builder 构建得到的不可变结构来源，负责：

- 保存规范化后的 `SystemDefinition` 列表；
- 保持 Pipeline 的宏观顺序；
- 提供合法性状态；
- 根据定义创建新的运行时 Schedule。

唯一的高层创建接口是：

```cpp
SystemSchedule CreateSchedule() const;
```

当前 `CreateSchedule()` 的语义是：

```text
创建 System 实例
    -> 反序列化配置
    -> 构建 Begin 和各 Phase 执行计划
    -> 封印 Schedule 拓扑
    -> 返回 Idle、未 Attach 的 Schedule
```

`SystemPipeline` 不拥有运行时 System 实例，也不负责 Attach、Begin、Run、End 或 Detach。

### 2.4 SystemSchedule

`SystemSchedule` 是一个已经构建完成的运行时执行对象，负责：

- 拥有 System 实例；
- 保存 Attach、Begin 和各 Update Phase 的索引计划；
- 执行 Attach、Begin、Run、End 和 Detach；
- 记录本次实际成功 Begin 的 System；
- 防止重复生命周期调用；
- 在拓扑构建完成后禁止结构修改。

当前 Schedule 使用 `SystemIndex` 连接条目和执行计划：

```text
m_Systems
m_AttachSequence
m_BeginPlan
m_PhasePlans
m_BegunSystems
```

Begin 的执行顺序和各 Update Phase 的执行顺序可以独立保存。当前实现仍然使用 System 的宏观 Order 对这些计划排序，但不预设未来所有阶段必须永远共用同一个 Order。

Schedule 的当前状态只有：

```text
Idle
Active
```

`Idle / Active` 只表示运行会话状态，不表示 Schedule 是否允许编辑拓扑。Schedule 构建完成后，拓扑始终不可修改。

Schedule 当前还提供：

- `FindSystemInstance()`：按 RuntimeId 或 TypeKey 查找具体实例；
- `GetRuntimeViews()`：生成不拥有实例的观察视图；
- `GetSystems() const`：提供只读 System 条目访问。

这些接口只提供观察和实例访问能力，不提供 GUI 批量转发，也不提供结构修改接口。

### 2.5 World

`World` 表示一个 Scene 上的运行时对象，负责：

- 持有当前 Scene；
- 持有一个运行时 Schedule；
- 创建 Schedule 后负责 Attach；
- 启动、更新和结束运行会话；
- 进行模式切换；
- 在 Apply 时决定旧 Schedule 和候选 Schedule 的生命周期时机；
- 向上层提供通用的 System 实例访问。

当前构造流程为：

```text
World 创建
    -> Pipeline::CreateSchedule()
    -> World Attach Schedule
```

当前 Apply 流程为：

```text
先完整构建候选 Schedule
    -> 结束旧运行会话
    -> 替换 Schedule
    -> Attach 新 Schedule
    -> 如果原来正在运行，则 Begin 新 Schedule
```

World 不提供 `OnGui()` 或其它 System GUI 包装接口。具体 System 的编辑交互属于 Ayin-Editor。

Schedule 析构时仍会执行必要的 Detach 清理；World 的生命周期接口和 Schedule 的清理接口将在后续阶段继续明确边界。

### 2.6 EditorSession

`EditorSession` 是编辑器级别的会话协调对象，负责：

- 持有当前场景的持久 `SystemPipeline`；
- 持有持续存在的 `EditorWorld`；
- 按需创建临时 Simulation / Runtime World；
- 提供当前被观察的 World；
- 统一决定结构 Apply 的目标；
- 管理临时 World 的结束和释放。

当前关系为：

```text
EditorSession
├── 持久 SystemPipeline
├── EditorWorld
└── 临时 Simulation / Runtime World
```

结构 Apply 的当前语义是：

```text
EditorWorld Apply
    -> 直接替换 EditorWorld Schedule

Simulation / Runtime Apply
    -> 结束临时 World
    -> Apply 到 EditorWorld
    -> 更新持久 Pipeline
    -> 后续临时 World 使用新 Pipeline
```

临时 World 的 System 实例在结束后释放，不尝试恢复已经结束的临时运行状态。

### 2.7 Ayin-Editor

Ayin-Editor 负责决定：

- 当前观察哪个 World；
- 当前选中哪个 System；
- 当前处于 Monitor 还是结构 Editor 状态；
- 何时允许创建 Builder、修改结构和 Apply；
- 何时直接调用具体 System 的 `OnEditorGui()`。

具体 System 的接口是：

```cpp
virtual void OnEditorGui() {};
```

Core 不负责批量调用该接口，也不负责替 Editor 阻止非法交互。Editor 必须自行保证：

- Monitor 状态只能修改当前实时 System 的参数；
- 结构性的 Phase、Mode、Order、添加和删除操作只能作用于 Builder；
- RuntimeView 不被用于修改 Schedule 拓扑；
- Apply 不发生在 Schedule 当前执行遍历期间。

---

## 3. 已完成工作

### 3.1 统一 Schedule 创建语义

已完成：

- 删除 `CreateDetachedSchedule()`；
- `CreateSchedule()` 只创建已构建、Idle、未 Attach 的 Schedule；
- World 构造时负责 Attach；
- Preview 也使用同一个创建入口；
- Pipeline 不再隐式触发 Attach。

这样避免了“普通 Schedule”和“Detached Schedule”同时存在所造成的创建语义分裂。

### 3.2 收敛 System Editor 交互接口

已完成：

- `ISystem::OnGui()` 更名为 `ISystem::OnEditorGui()`；
- 删除 `World::OnGui()`；
- 删除 `SystemSchedule::OnGui()`；
- 删除 `SystemPipelineEditor::OnGui()`；
- 增加按 RuntimeId / TypeKey 获取 System 实例的通用接口；
- Editor 通过具体实例自行调用 `OnEditorGui()`；
- Core 不再承担 System GUI 的批量调度和异常包装。

其它模块中的 `OnGui()`，例如 EditorLayer、Component 和 ScriptableEntity，属于它们各自的显示职责，本阶段没有修改。

### 3.3 建立 Preview 隔离和多 World Apply 基础

已完成：

- Preview Schedule 只创建实例和反序列化配置；
- Preview 不执行 Attach、Begin、Update、End；
- Preview System 不复用实时 World 的 System 实例；
- Preview 的 `OnEditorGui()` 不直接影响实时实例；
- Cancel 可以直接丢弃 Builder 和 Preview；
- Simulation / Runtime Apply 前先结束临时 World；
- 结构 Apply 的目标统一为 EditorWorld；
- Apply 成功后持久 Pipeline 更新；
- 后续创建的临时 World 使用新的 Pipeline。

### 3.4 已完成的验收基础

Sandbox 当前覆盖了以下行为：

- System 注册和实例创建；
- 自动 Order 和显式 Order；
- 各 Phase 执行顺序；
- Mode 过滤；
- SystemContext 转发；
- Begin / End 反向顺序；
- 重复添加和拓扑冻结；
- Schedule 移动；
- 直接取得具体 System 实例并调用 `OnEditorGui()`；
- Preview 和实时实例隔离；
- World Apply 生命周期；
- EditorSession 多 World Apply；
- System 配置序列化和反序列化；
- Phase / Mode mask 以具体枚举名称序列化。

已通过：

```text
git diff --check
```

当前分支代码修改均已提交，只有 `System接口收敛实施记录.md` 保持未跟踪状态，作为本地实施记录。

---

## 4. 当前缺口与明确限制

### 4.1 仍然必须实现

以下内容尚未真正接入 Ayin-Editor：

- Pipeline 面板；
- Monitor / Editor 状态切换；
- 当前活动 World 的 RuntimeView 显示；
- Builder 的结构显示和编辑；
- Preview System 的显示；
- 具体 System 的 `OnEditorGui()` 面板调用；
- Apply / Cancel 交互；
- 安全时机下的 EditorSession Apply。

System 序列化的基础能力已经存在，但以下整理尚未完成：

- JSON DTO 文件结构重新分层；
- SystemPipeline、SystemSchedule 和 World Serializer 的职责重新核对；
- Glaze metadata 与领域类型的头文件依赖整理；
- 反序列化错误传播路径统一；
- 潜在的头文件递归引用整理。

### 4.2 需要重新设计或收敛

#### `World::ApplyPipeline()` 的失败边界

当前候选 Pipeline 和候选 Schedule 会在旧 Schedule 停止前完成构建，因此构建失败不会破坏旧 World。

但是，如果新 Schedule 在 Attach 或 Begin 阶段失败，当前实现不能完整恢复已经结束的旧 Schedule。后续需要在“完整回滚”和“明确失败后保持 World Idle”之间做出最终决定，并写入接口契约。

#### 安全帧边界

当前 Apply 由调用者保证不发生在 Schedule 当前遍历期间。代码中还没有 Pending Apply、帧末提交或执行期间保护机制。

后续需要决定：

- 由 Ayin-Editor / EditorSession 保证调用时机；
- 或由更高层提供延迟提交机制。

本阶段不应为了这个问题引入复杂的跨线程或事务框架。

#### RuntimeView 的归属

当前 `SystemRuntimeView` 位于 Core 的 `SystemSchedule.h` 中，并由 World 转发获取。

它没有修改能力，也不是 GUI 转发，但它的命名和归属仍然偏向 Editor。后续应重新判断：

- 是否保留为 Core 的通用运行时观察类型；
- 是否改名为更通用的 Runtime 信息视图；
- 是否将 Editor 专用的包装类型放到 Ayin-Editor。

#### Schedule 清理接口

当前同时存在：

```cpp
AttachSystems()
DetachSystems()
ClearSystems()
Shutdown(context)
```

Schedule 析构和 Move assignment 也会参与 Detach 清理。后续需要明确：

- World 是不是唯一可以驱动生命周期的外部对象；
- Shutdown 是否成为正式的清理入口；
- 析构函数是否只承担最后的尽力清理；
- `m_AttachSequence` 是运行时成功记录，还是构建阶段的固定计划。

#### 观察接口的范围

当前 `GetSystems() const`、`GetRuntimeViews()` 和 `FindSystemInstance()` 都是只读观察或实例访问接口。

后续需要避免两种极端：

- 为了防止误用而引入过多包装层；
- 为了方便而暴露能够破坏 Schedule 拓扑的可写内部结构。

---

### 4.3 当前明确不做

本轮后续阶段不实现：

- System 实例增量复用；
- Apply 时跨 System 迁移运行状态；
- 多线程调度；
- System 依赖图和拓扑排序；
- 项目级 Pipeline 资源系统；
- 全局反射式 Editor 配置系统；
- Scene / Component 序列化全面重写；
- 为所有对象引入复杂 Generation Handle；
- 为了消除当前少量裸指针而建立复杂 Handle 层。

这些方向可以作为未来路线记录，但不应混入当前接口收敛和 Editor 接入阶段。

### 4.4 当前环境限制

当前尚未完成 MSVC 全量构建。已知原因是仓库记录的 ImGui 子模块提交：

```text
07d42bf753fbeca95fa51540029623d6d1c26dea
```

在当前远端不可用，导致依赖初始化和工程生成无法完整完成。

这不替代代码验收。每个代码阶段仍需独立执行：

- `git diff --check`；
- 可执行的 Sandbox 验收；
- 条件允许时的 MSVC 构建；
- 若构建受依赖限制，则记录具体原因。

---

## 5. 下一阶段实施顺序

以下阶段是本文档重写之后的正式实施顺序。阶段编号只服务于当前路线，不延续旧规划中已经失真的编号。

### 阶段 1：确认并收敛当前生命周期接口

目标：

- 明确 World 和 Schedule 的生命周期所有权；
- 明确 Attach、Detach、Shutdown 和析构之间的关系；
- 明确 Apply 在不同失败阶段的最终状态；
- 解决或正式保留 `m_AttachSequence` 的生成时机；
- 不引入新的复杂生命周期抽象。

工作内容：

- 检查 Schedule 析构、Move assignment 和 World 析构的交互；
- 统一重复 Begin、End、Attach、Detach 的行为；
- 确认 Clear / Shutdown 是否仍然需要同时保留；
- 为 Apply 失败状态增加清晰的结果和测试；
- 补充旧 Schedule、新 Schedule 的生命周期轨迹测试。

验收标准：

- 重复 Begin、End、Attach、Detach 不重复触发回调；
- Schedule 析构不造成重复 Detach；
- World Apply 的旧、新生命周期顺序稳定；
- 候选构建失败时旧 World 不变；
- Attach / Begin 失败时结果符合明确的接口契约；
- Move 不造成源对象或目标对象的重复清理。

### 阶段 2：整理 Runtime 观察接口

目标：

- Core 只提供通用观察和实例访问能力；
- 不在 World 或 Schedule 中增加 GUI 转发；
- 确认 `SystemRuntimeView` 的最终归属和命名；
- 让 Editor 能够观察 System，但不能通过观察接口修改 Schedule 拓扑；
- 保持访问接口简洁。

工作内容：

- 评估 `GetSystems()`、`GetRuntimeViews()` 和 `FindSystemInstance()` 的重叠；
- 确认观察数据是否需要稳定的 ID、TypeKey 和 Specification；
- 确认 RuntimeView 是否需要包含配置数据，还是只包含运行时信息；
- 让 Editor 的限制由 Editor 工作流保证，而不是在 Core 中加入 GUI 专用防护。

验收标准：

- Editor 可以列出当前 World 中的 System；
- Editor 可以取得选中的具体 System 实例；
- 参数修改只作用于实例；
- Phase、Mode、Order 修改必须通过 Builder；
- RuntimeView 不会修改当前 Schedule 拓扑；
- 不新增 World / Schedule 的 System GUI 包装接口。

### 阶段 3：整理 Builder、Pipeline 和序列化边界

目标：

- 明确 Builder 是唯一结构编辑模型；
- 明确 Pipeline 是持久结构来源；
- 明确 ScheduleSerializer 的输入输出边界；
- 继续把枚举 mask 序列化为具体枚举名称；
- 整理 JSON DTO、Glaze metadata 和头文件依赖。

当前期望的数据流：

```text
实时 Schedule
    -> SystemPipelineJson
    -> Builder
    -> 编辑结构
    -> Pipeline
    -> 新 Schedule
```

工作内容：

- 保留 `SystemJson`、`SystemPipelineJson` 作为数据结构，不让 DTO 主动查询 Registry；
- 明确 `SystemScheduleSerializer` 只负责数据映射，不负责文件 I/O；
- 反序列化后先生成 Builder，再由 Builder 构建 Pipeline；
- 对未知 TypeKey、重复 System 和配置反序列化失败提供清晰错误传播；
- 拆分可能造成递归依赖的 JSON 头文件和 Glaze metadata；
- 检查 `SceneMode`、`SystemPhase` 的 mask round-trip。

验收标准：

- Schedule -> JSON -> Builder 能够完成；
- Builder -> Pipeline -> Schedule 能够完成；
- Phase / Mode mask 的语义 round-trip 不变；
- JSON 中保存具体枚举名称，不保存底层整数 mask；
- 未知 TypeKey、重复 System 和错误配置能够失败；
- 失败时不产生可继续使用的半成品 Pipeline；
- Builder 构建 Pipeline 后仍可继续编辑；
- Pipeline 对外不可修改。

### 阶段 4：接入 Ayin-Editor Pipeline 面板

目标：

- Monitor 状态观察当前活动 World；
- Editor 状态创建 Builder 和 Preview；
- 左侧显示 RuntimeView 或 Builder 定义；
- 右侧由 Editor 直接调用当前选中 System 的 `OnEditorGui()`；
- Cancel 丢弃 Builder 和 Preview；
- Apply 提交到 EditorSession。

Monitor 状态：

```text
当前活动 World
    -> 只读观察当前 Schedule
    -> 获取选中 System 实例
    -> 直接调用具体 System::OnEditorGui()
    -> 参数实时作用于当前实例
```

Editor 状态：

```text
当前活动 World 的 Schedule
    -> 序列化为 JSON DTO
    -> 反序列化为 Builder
    -> 创建未运行的 Preview Schedule
    -> Editor 修改 Builder 和 Preview
    -> Apply 或 Cancel
```

验收标准：

- Monitor 下参数修改实时作用于当前活动 World；
- Monitor 下不能修改结构；
- Editor 下的结构修改不污染实时 World；
- Preview 不执行运行时生命周期；
- Preview 的配置修改能够同步回 Builder；
- Cancel 丢弃全部 Preview 修改；
- Apply 后旧 Schedule 完整 End / Detach，新 Schedule 正确 Attach / Begin；
- Simulation / Runtime Apply 返回 EditorWorld。

### 阶段 5：安全帧边界与完整 Apply 语义

目标：

- 明确 Apply 的安全时机由哪一层保证；
- 保证候选数据和候选 Schedule 在旧 Schedule 停止前完成构建；
- 为 Attach / Begin 失败定义最终行为；
- 必要时保留旧 Pipeline 和旧 World 状态；
- 不实现运行状态迁移。

候选 Apply 的基本原则：

```text
校验 Pipeline
    -> 创建实例
    -> 反序列化配置
    -> 构建执行计划
    -> 候选完整性检查
    -> 停止旧 Schedule
    -> 提交候选 Schedule
```

验收标准：

- 不会在 Schedule 当前执行遍历期间替换；
- 候选构建失败时旧 World 保持不变；
- Apply 的生命周期轨迹可预测；
- Simulation / Runtime Apply 不直接恢复已结束的 System 实例；
- Apply 成功后 EditorSession 的持久 Pipeline 与 EditorWorld 保持一致。

---

## 6. 阶段验收与提交规则

每完成一个代码阶段：

- 先补充或调整对应 Sandbox 验收；
- 执行 `git diff --check`；
- 条件允许时执行 MSVC 构建；
- 记录依赖或环境造成的构建限制；
- 单独提交，不把多个阶段压入一个大提交。

提交信息保持项目现有风格：

```text
- 重构: 简略概括修改

# 详细说明本次修改所依据的原则
# 说明涉及的文件、模块和行为变化
```

其中：

- `-` 用于简略概括或精妙总结；
- `#` 用于解释改动原则、具体模块和行为变化。

本次文档重写只修改 `规划.md`，不提交 `System接口收敛实施记录.md`。

---

## 7. 长期路线索引

以下方向暂时只作为路线记录，不属于当前实施阶段：

- 项目级 Pipeline 资源；
- 多场景共享 Pipeline；
- Pipeline 继承与场景覆盖；
- Pipeline 版本和资源引用；
- System 实例增量复用；
- Apply 时保留未变化 System 的运行状态；
- 各 Phase 独立 Order；
- System 依赖图和拓扑排序；
- 多线程调度；
- Generation Handle；
- 更通用的运行时资源身份系统。

这些方向只有在出现真实使用需求后再单独设计，不提前为它们引入复杂抽象。